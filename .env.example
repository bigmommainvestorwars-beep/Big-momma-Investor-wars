# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Application Environment: 'development' | 'staging' | 'production'
VITE_APP_ENV="development"

# Firebase Client Configuration (public web config - safe in client bundle)
# Targets the (default) Cloud Firestore database automatically.
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""
VITE_FIREBASE_PROJECT_ID=""
VITE_FIREBASE_STORAGE_BUCKET=""
VITE_FIREBASE_MESSAGING_SENDER_ID=""
VITE_FIREBASE_APP_ID=""

# Server-Side Cloud Functions Architecture:
# Deployed Cloud Functions use Google Cloud Application Default Credentials (ADC) natively.
# No manual service account key or private key credentials are required in production.
