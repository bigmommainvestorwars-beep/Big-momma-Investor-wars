/**
 * HARD-CODED MULTIPLAYER CONNECTIVITY TEST CONFIGURATION
 * 
 * Temporary configuration for multiplayer connectivity testing.
 * Binds the fixed room code BM-0X9X to a single authoritative Firebase lobby.
 * 
 * To disable test mode, set IS_TEST_ROOM_MODE = false.
 */

export const TEST_ROOM_CODE = 'BM-0X9X';
export const TEST_MATCH_ID = 'match_test_bm_0x9x';
export const IS_TEST_ROOM_MODE = false;
